# 人类在环：先提议后提交

> 2026 年对 HITL 的共识是具体的。它不是"代理询问，用户点击批准"。它是先提议后提交：提议的动作被持久化到带有幂等键的持久存储；呈现给审查者，包含意图、数据血统、触及的权限、爆炸半径和回滚计划；仅在积极确认后提交；执行后验证以确认副作用确实发生。LangGraph 的 `interrupt()` 加 PostgreSQL 检查点、Microsoft Agent Framework 的 `RequestInfoEvent` 和 Cloudflare 的 `waitForApproval()` 都实现了相同的形状。典型的失败模式是橡皮图章审批：未经审查点击"批准？"。记录的缓解措施是带有明确清单的质询-响应。

**类型：** 学习
**语言：** Python（标准库，带幂等性的先提议后提交状态机）
**前置条件：** 第 15 阶段 · 12（持久执行），第 15 阶段 · 14（绊线）
**时间：** ~60 分钟

## 问题

代理采取一个动作。用户必须决定：批准还是不批准。如果决定是即时的，它可能不是审查。如果决定是结构化的，它慢但可信。工程问题是如何使结构化审查成为最小阻力路径。

2023 年时代的 HITL 模式是同步提示："代理想向 X 发送内容为 Y 的邮件——批准？"用户点击批准。每个人都觉得系统安全。实际上这个表面被大量橡皮图章化：用户快速批准，批准预测很少，当代理出错时，审计跟踪显示用户无法回忆的长批准历史。

2026 年模式——先提议后提交——将 HITL 移到持久基板上，附加结构化元数据，并要求积极提交。每个托管代理 SDK 都发布了一个版本：LangGraph `interrupt()`、Microsoft Agent Framework `RequestInfoEvent`、Cloudflare `waitForApproval()`。API 名称不同；形状相同。

## 概念

### 先提议后提交状态机

1. **提议。** 代理产生拟议动作。持久化到持久存储（PostgreSQL、Redis、Durable Object）。包括：
   - 意图（代理为什么做这个）
   - 数据血统（什么来源导致此提议）
   - 触及的权限（哪些范围/文件/端点）
   - 爆炸半径（最坏情况是什么）
   - 回滚计划（如果提交了，我们如何撤销）
   - 幂等键（每个提议唯一；重新提交返回相同记录）
2. **呈现。** 审查者看到带有所有元数据的提议。审查者是人（不是代理审查自己）。
3. **提交。** 积极确认。动作执行。
4. **验证。** 执行后，回读副作用并确认。如果验证步骤失败，系统处于已知坏状态，告警介入。

### 幂等键

没有幂等键，瞬态故障后的重试可能双重执行已批准的动作。具体示例：用户批准"从 A 转账 100 美元到 B"。网络闪烁。工作流重试。用户已批准一次但转账执行两次。幂等键将批准绑定到单个唯一副作用；第二次执行是空操作。

这与 Stripe 和 AWS API 使用的幂等模式相同。在 Microsoft Agent Framework 文档中明确将此重用于代理批准。

### 持久性：为什么批准比进程更持久

批准候诊室是代理不拥有的状态。工作流暂停（第 12 课）。当批准到达时，工作流从该点精确恢复。这就是为什么 LangGraph 将 `interrupt()` 与 PostgreSQL 检查点配对，而不仅仅是内存状态——两天后的批准仍然找到完整的工作流。

### 橡皮图章审批和质询-响应缓解

HITL 的默认 UI（"批准"/"拒绝"按钮）产生没有真正审查的快速批准。记录的缓解措施：质询-响应清单，在批准按钮启用前要求对特定问题的积极回答。具体形状：

- "你理解这触及什么资源吗？[ ]"
- "你验证了爆炸半径可接受吗？[ ]"
- "如果这失败了你有回滚计划吗？[ ]"

不是为了官僚主义而官僚主义——是一个强制函数。无法勾选框的审查者要么要求澄清（升级）要么拒绝（安全默认）。Anthropic 代理安全研究明确引用清单驱动的 HITL 作为橡皮图章审批模式的缓解措施。

### 什么算作重要的

不是每个动作都需要先提议后提交。2026 年指南：

- **重要动作**（总是 HITL）：不可逆写入、金融交易、出站通信、生产数据库更改、破坏性文件系统操作。
- **可逆动作**（有时 HITL）：本地文件编辑、暂存环境更改、带有明确回滚的可逆写入。
- **读取和检查**（从不 HITL）：读取文件、列出资源、调用只读 API。

### 动作后验证

"提交运行了"不等于"副作用发生了"。网络分区和竞争条件可能产生一个认为自己成功了但后端没有持久化的工作流。验证步骤在提交后重新读取目标资源以确认。这与带有 `RETURNING` 子句的数据库事务或 `PutObject` 后的 AWS `GetObject` 是相同的模式。

### EU AI Act 第 14 条

第 14 条要求对欧盟高风险 AI 系统进行有效的人类监督。"有效"不是装饰性的。监管语言明确排除橡皮图章模式。带有质询-响应的先提议后提交是在 Microsoft Agent Governance Toolkit 合规文档中经得起第 14 条审查的形状。

## 使用它

`code/main.py` 在标准库 Python 中实现先提议后提交状态机。持久存储是 JSON 文件。幂等键是 (thread_id, action_signature) 的哈希。驱动器模拟三种情况：干净的审批流程、瞬态故障后的重试（不能双重执行）以及橡皮图章默认 vs 质询-响应流程。

## 交付它

`outputs/skill-hitl-design.md` 审查拟议 HITL 工作流的先提议后提交形状，并标记缺失的元数据、幂等性、验证或质询-响应层。

## 练习

1. 运行 `code/main.py`。确认已批准提议的重试使用持久记录且不重新执行。现在将幂等键更改为包含时间戳并显示重试双重执行。

2. 用 `rollback` 字段扩展提议记录。模拟验证步骤失败的执行。显示回滚自动触发。

3. 阅读 Microsoft Agent Framework 的 `RequestInfoEvent` 文档。识别 API 包含的一个玩具引擎缺少的元数据字段。添加它并解释它防止什么。

4. 为特定动作设计质询-响应清单（例如"发布到公共 Twitter 帐户"）。审查者必须回答哪三个问题？为什么是这三个？

5. 选择一个同步"批准？"提示足够的情况（不需要持久存储）。解释原因，并命名你接受的风险类别。

## 关键术语

 | 术语 | 人们怎么说 | 实际含义 |
|---|---|---|
| 先提议后提交 | "两阶段审批" | 持久化提议 + 积极提交 + 验证 |
| 幂等键 | "重试安全令牌" | 每个提议唯一；第二次执行空操作 |
| 数据血统 | "来自哪里" | 导致提议的特定源内容 |
| 爆炸半径 | "最坏情况" | 动作出错时的影响范围 |
| 橡皮图章 | "快速审批" | 未经真正审查点击"批准" |
| 质询-响应 | "强制清单" | 审查者必须积极确认特定问题 |
| RequestInfoEvent | "MS Agent Framework 原语" | 带结构化元数据的持久 HITL 请求 |
| `interrupt()` / `waitForApproval()` | "框架原语" | LangGraph / Cloudflare 相同形状的等效物 |

## 延伸阅读

- [Microsoft Agent Framework — Human in the loop](https://learn.microsoft.com/en-us/agent-framework/workflows/human-in-the-loop) — `RequestInfoEvent`、持久批准。
- [Cloudflare Agents — Human in the loop](https://developers.cloudflare.com/agents/concepts/human-in-the-loop/) — `waitForApproval()` 和 Durable Objects。
- [Anthropic — 实践中测量代理自主性](https://www.anthropic.com/research/measuring-agent-autonomy) — HITL 作为长时域风险的缓解措施。
- [EU AI Act — 第 14 条：人类监督](https://artificialintelligenceact.eu/article/14/) — 高风险系统的监管基线。
- [Anthropic — Claude 的宪法（2026 年 1 月）](https://www.anthropic.com/news/claudes-constitution) — 围绕监督的宪法框架。
